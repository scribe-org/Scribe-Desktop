{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "Debug",
            "program": "${workspaceFolder}/scribe/target/debug/scribe",
            "args": [],
            "cwd": "${workspaceFolder}"
        }
    ]
}
